{
  "name": "stagecut",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "dev": "pnpm --filter @stagecut/studio run dev",
    "lint": "pnpm exec biome check .",
    "test": "pnpm -r --if-present run test",
    "type-check": "pnpm -r --if-present run type-check"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/node": "^24.12.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  },
  "packageManager": "pnpm@10.25.0"
}
